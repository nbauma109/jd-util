class SwitchPatternMatchingMultiLabelsWhen {
    static void describe(Object o) {
        switch (o) {
            case Integer i, Long l when o != null && o.hashCode() > 0 -> System.out.println(1);
            default -> System.out.println(0);
        }
    }
}
